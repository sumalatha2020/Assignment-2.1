#Accepts comma,seperated values from user
values = input("Input comma seprated numbers : ")
#split with "," and stores the values in list variable as list of values
list = values.split(",")
print('List : ',list)